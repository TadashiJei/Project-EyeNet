// project imports
import dashboard from './dashboard';
import monitoring from './monitoring';
import settings from './settings';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, monitoring, settings]
};

export default menuItems;
